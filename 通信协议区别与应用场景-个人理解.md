# 通信协议的个人理解：把芯片当成一群人在聊天

> 学单片机之后，跟各种传感器、模块打交道多了，我发现通信协议这东西，本质上就是**人与人之间交流的规则**。两个人说话要约定语言、语速、谁先说，芯片之间"说话"也一样。下面是我自己的一套理解，用生活化的方式记下来，方便以后复习，也分享给一起学单片机的朋友。

## 一、UART（串口）：两个人用对讲机

UART 是我接触最早的协议，也是最"直白"的。

想象一下：你和对讲机那头的同事聊天。对讲机是半双工（只能一边说一边听），但 UART 更高级一点，它是两条线，一条专门说（TX），一条专门听（RX），**可以一边说一边听**（全双工）。

关键点：**你们俩没有信号灯，全靠约定好语速**。UART 是异步的，没有时钟线，收发双方必须提前约定波特率（比如 9600），就像说好"一分钟讲 200 字"。不然你噼里啪啦讲一堆，对方根本听不清你在说啥——串口乱码就是这么来的，波特率对不上，等于你跟一个语速完全不同的外星人讲话。

特点：一对一、简单、通用；速度一般，距离不远。

应用场景：调试日志（printf 大法）、GPS 模块、蓝牙串口模块、ESP8266。我调代码时最爱用它——单片机干了啥，一行 printf 全知道，比看寄存器快多了。

## 二、I2C：老师点名提问

I2C 只有两根线：SCL（时钟）和 SDA（数据），却能挂一堆设备。我第一次用的时候很懵：就两根线，怎么接这么多传感器？

想象一下：教室里老师要提问，先敲两下黑板（起始信号），然后喊学号："5 号同学，回答一下！"（发送设备地址）。5 号听到自己名字，站起来应答（拉低 SDA 回 ACK），答完坐下。老师接着问下一个。全班都坐在教室里（都挂在总线上），但**只有被点到名字的那个才开口**。

这就是 I2C 的核心：**靠地址找人，一根数据线轮流说话（半双工）**。SCL 时钟线相当于老师的节拍器，什么时候说、什么时候听，都跟着节拍走，所以叫"同步"。

特点：两根线挂多设备、靠地址寻址、速度中等（标准 100k / 快速 400k）、半双工。代价是总线上人多，节奏稍慢，一慢就容易被干扰（所以线不能太长）。

应用场景：温度/湿度传感器（AHT20 等）、EEPROM（AT24C02）、OLED 屏、MPU6050 陀螺仪。我读传感器基本都用它，两根线接过去，写地址读数据，方便得很。

## 三、SPI：领导一对一面试

SPI 是四根线起步：SCLK（时钟）、MOSI（主发从收）、MISO（主收从发）、CS（片选）。它比 I2C 快得多，但每多一个设备，就要多占一根片选线。

想象一下：领导要面谈几个下属，每次只叫一个人进办公室（CS 片选线拉低，选中谁谁开口），门一关，其他人外面等着。办公室里，领导问一句（MOSI），下属答一句（MISO），**问和答可以同时进行**（全双工），语速极快（时钟频率拉满）。面谈完一个，再叫下一个。

所以 SPI 的特点是：**全双工、速度快、一对一（或一主多从但各占片选）**。速度上 SPI 是"跑车"，I2C 是"小电驴"。代价是**线多**，接一个设备 4 根线，接三个设备就得 6 根（片选分开算）。

应用场景：SD 卡、SPI Flash（W25Q64）、TFT 显示屏、高速传感器。我刷屏时用 SPI，比 I2C 明显快一大截，刷动画都不卡。

## 四、CAN：会议室里抢话

CAN 用在汽车、工业现场最多，它的精髓是**仲裁**。

想象一下：会议室里大家自由发言，但如果两个人同时开口怎么办？CAN 的规则是：谁的 ID 优先级高（数字小），谁继续说，另一个自动闭嘴等下一轮。所以每个人开口前都带着"身份牌"，大佬先说，小弟让路。这就是它"多主"的含义——**没有绝对的老师，谁急谁先说**。

而且 CAN 是差分信号（CANH/CANL 两根线差着电压传），就像用两个喇叭对着喊，抗干扰能力特别强。在发动机旁边这种"电闪雷鸣"的环境里也能稳得住。

特点：多主、带仲裁、差分抗干扰、距离长、节点多。代价是协议本身比 UART/I2C/SPI 复杂，得用带 CAN 控制器的芯片，学起来门槛高一截。

应用场景：汽车 ECU/OBD、工业控制、机器人伺服驱动器。

## 五、RS-485：一群人排队打电话

RS-485 常跟 MODBUS 协议一起出现，物理层是差分，能传上千米。

想象一下：一个班只有一部电话（一根双绞线），谁要打电话先拨号（设备地址），然后占线说话，说完别人才能用。所以 RS-485 是**半双工**，而且必须"排队讲话"——两个人同时说，线上一片混乱，谁也听不清。

特点：半双工、长距离（可达 1200 米）、多节点（最多几十个）、抗干扰好、速度一般。

应用场景：工业现场仪表、变频器、电表，MODBUS-RTU 协议满天飞。工控里见得最多。

## 六、USB：城市快递系统

USB 太常见了，键盘、鼠标、U 盘都是它。它更像**快递物流系统**：主机（物流中心）要先给每个设备"分配地址"（枚举，设备插入时主动上报身份），然后按"数据包"（包裹）运送数据，一个包裹一个包裹地发，分门别类、有头有尾。

特点：主机-设备树状结构、即插即用（热插拔）、速度飞快（USB 2.0 / 3.0 差别巨大）、协议复杂。所以 USB 一般不用在单片机互连上，太"重"了——你见过两个单片机用 USB 互传数据吗？少见，成本高还麻烦。

应用场景：键盘鼠标、U 盘、摄像头、调试器（ST-Link/DAP-Link 本质就是 USB 转出来的）。

## 七、怎么选？我的个人经验

| 协议 | 比喻 | 线数 | 方向 | 速度 | 距离 | 典型场景 |
|------|------|------|------|------|------|----------|
| UART | 对讲机 | 2 | 全双工 | 慢-中 | 短 | 调试、蓝牙、GPS |
| I2C | 老师点名 | 2 | 半双工 | 中 | 短 | 传感器、EEPROM、OLED |
| SPI | 一对一面试 | 4+ | 全双工 | 快 | 短 | Flash、SD 卡、显示屏 |
| CAN | 会议室抢话 | 2 | 半双工 | 中-快 | 长 | 汽车、工业总线 |
| RS-485 | 排队打电话 | 2 | 半双工 | 中 | 很长 | 工业现场、MODBUS |
| USB | 快递物流 | 4 | 全双工 | 很快 | 短 | 电脑外设 |

我的选择口诀：

- **就想省线、挂一堆传感器** → I2C（两根线解决战斗）
- **要速度快、数据量大** → SPI（跑车，就是线多）
- **图简单、方便调试** → UART（最朴素最可靠）
- **环境恶劣、距离远、要稳定** → RS-485 / CAN
- **跟电脑打交道** → USB，没得选

## 结语

其实协议没有绝对的好坏，只有合不合适。就像沟通一样：宿舍里叫室友用吼（UART），公司点名用话筒（I2C），领导单独谈话去办公室（SPI），高速公路上开车靠秩序（CAN）……选对了，代码写得舒心；选错了，调通信调到怀疑人生。这也是我踩过不少坑之后悟出来的。希望这份"人话版"理解能帮到同样在学单片机的你。
