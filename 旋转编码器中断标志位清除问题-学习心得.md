# 旋转编码器中断标志位清除问题——学习心得

> 一个由「一行代码的位置」引发的 Bug，让我重新理解了中断服务函数的执行时序。

## 一、出现问题

最近在学 STM32 的外部中断（EXTI），我拿了一个旋转编码器（Rotary Encoder）做计数实验：**正转加一、反转减一**。编码器的 A、B 两相分别接到 GPIO，其中 B 相接 `EXTI0`（下降沿触发），计划在中断服务函数里读 A 相电平来判断旋转方向。

代码写好后烧录进去，硬件表现立刻不对劲：

1. **正转时，计数器只能 +1**——不管我把编码器转多少格，读数始终停在 1；
2. **反转时，计数器直接跳成 65535**——本应该减一，却蹦出一个大得离谱的数。

一个"正转 +1、反转 -1"的简单逻辑，硬件却完全不听使唤，我第一反应是接线错了或者编码器坏了。

## 二、发现问题

我先把编码器、杜邦线和引脚都换了一遍，现象依旧，排除了硬件问题。于是把 `Encoder_Count` 的值通过串口打印出来，正转、反转各试了几次，发现规律很稳定：

- 正转：计数被"锁死"在 1，再多转也不涨；
- 反转：计数瞬间从 0 跳到 65535。

65535 这个数很眼熟——`uint16_t` 类型的最大值，也就是 `0xFFFF`。`Encoder_Count` 声明成 `uint16_t`，那么 `0 - 1` 就会下溢成 65535。这说明**反转时其实一直在执行"减一"，而且执行了很多次**，减到 0 之后继续减，直接下溢了。

问题不在硬件，在中断的处理逻辑上。我回过头逐行检查中断服务函数，发现清标志位的位置写错了：

```c
// 错误版本：清标志位写在了 if 判断内部
void EXTI0_IRQHandler(void)
{
    if (EXTI_GetITStatus(EXTI_Line0) == SET)      // 检测中断标志
    {
        if (GPIO_ReadInputDataBit(GPIOB, GPIO_Pin_1) == 0)  // 读 A 相电平判方向
        {
            Encoder_Count++;
            EXTI_ClearITPendingBit(EXTI_Line0);   // ❌ 清标志位被包在条件里
        }
    }
}
```

**根因就在这里**：`EXTI_ClearITPendingBit()` 只有在「中断标志已置位 **且** A 相为低电平」时才会被执行。一旦反转（A 相为高电平），程序走不进内层 `if`，中断挂起位就永远清除不掉。

EXIT 的中断挂起位只要还是 `SET`，中断服务函数一返回，NVIC 就会**立刻再次触发同一个中断**。于是反转时，一次物理边沿被反复"重入"，编码器 A 相电平在这期间来回跳变，被反复误判、反复计数，最终把计数减穿 0，下溢成 65535。

## 三、解决问题

问题定位清楚了，改法只有一行——**把清标志位移到 `if` 判断之外**，让它成为中断服务函数里无条件执行的"收尾动作"：

```c
// 正确版本：清标志位放在 if 之外，无条件执行
void EXTI0_IRQHandler(void)
{
    if (EXTI_GetITStatus(EXTI_Line0) == SET)
    {
        if (GPIO_ReadInputDataBit(GPIOB, GPIO_Pin_1) == 0)
        {
            Encoder_Count++;
        }
    }
    EXTI_ClearITPendingBit(EXTI_Line0);   // ✅ 无论方向如何，都清除挂起位
}
```

重新编译烧录后，正转稳定 +1、反转稳定 -1，计数完全恢复正常。

## 四、复盘反思

这个 Bug 前后折腾了大半天，最后只改了一行，收获却比写对一次代码大得多。几点反思记下来：

1. **中断标志位是"无条件收尾"，不是"条件业务"**。清标志位的作用是告诉硬件"这个中断我处理完了，别再触发"，它和业务逻辑解耦。把它塞进 `if` 里，等于让"是否处理完"依赖"业务分支是否命中"，一旦分支不命中，中断就永远"没处理完"，反复重入。

2. **挂起位不清，中断会重入**。这是这次现象的本质——不是编码器坏了，是同一个中断被触发了一次又一次。以后遇到"一次操作结果被放大很多倍 / 数值异常跳动"，先怀疑中断有没有被重复触发、标志位有没有清干净。

3. **看懂溢出信号**。`65535` 不是随机乱码，它是 `uint16_t` 的 `0 - 1` 下溢结果。从异常值反推数据类型和运算方向，能帮自己快速定位到"减穿零"这个事实。

4. **先用现象缩小范围，再怀疑硬件**。一开始我以为是接线和编码器的问题，白白换了一遍器件。其实把计数打出来一看，规律非常稳定，就应该果断回头查代码逻辑——稳定可复现的现象，往往指向确定性的软件 Bug。

5. **中断服务函数要尽量短、逻辑尽量简单**。这次也提醒我，ISR 里做方向判断这类带条件的处理时，尤其要分清哪些是"每次进入都必须做"的事（清标志位），哪些是"满足条件才做"的事（加减计数）。

这行代码的位置，本质上是我对"中断处理流程"理解不到位造成的。写下来，提醒自己也分享给遇到同样现象的朋友。
